import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  canvas: {
    alignItems: 'flex-end',
    height: 150,
    position: 'absolute',
    width: 44,
    zIndex: 1000,
  },
  wrapper: {
    zIndex: 2,
  },
});
