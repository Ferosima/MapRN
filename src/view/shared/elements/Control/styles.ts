import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  content: {
    alignItems: 'center',
    borderRadius: 12,
    flex: 1,
    height: '100%',
    justifyContent: 'center',
    position: 'absolute',
    width: '100%',
    zIndex: -1,
  },
  wrapper: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
