import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  canvas: {
    flex: 1,
    position: 'absolute',
    width: '100%',
    zIndex: -1,
  },
  wrapper: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
