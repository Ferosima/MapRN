import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  canvas: {
    alignItems: 'flex-end',
    height: 150,
    position: 'absolute',
    width: 44,
  },
  wrapper: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
